function calcLength (name) {
    console.log("dein Wort hat sovielen Zeichen" + name.length)
    }
    calcLength("John");
    calcLength("Angela Merkel");


  
    return "Der Name ist folgende Zeichen lang: " + name.length;