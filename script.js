function calcLength (name) {
    return "Der Name ist folgende Zeichen lang: ", name.length
};

calcLength ("John");
calcLength("Bob");

// Returns


let returnet = calcLength("John");
let returnet2 = calcLength("Bob");
let returnet3 = calcLength("Chuk");

console.log(returnet);
console.log(returnet2);
console.log(returnet3);